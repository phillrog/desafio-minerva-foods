name: CI/CD Desafio Minerva Foods 

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore
        run: dotnet restore ./src/DesafioMinervaFoods.slnx
      - name: Run Tests
        run: dotnet test ./src/DesafioMinervaFoods.slnx --no-restore

  build:
    runs-on: ubuntu-latest
    needs: test # Só compila se os testes passarem
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Build Project
        # Compila a solução para garantir que está tudo OK
        run: dotnet build ./src/DesafioMinervaFoods.slnx -c Release --no-restore

  deploy:
    runs-on: ubuntu-latest
    needs: build # Agora o deploy depende do build passar
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Install Railway CLI
        run: npm install -g @railway/cli
      - name: Deploy to Railway
        run: railway up -e production -s ${{ secrets.RAILWAY_SERVICE_ID }} --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: railway up -e production -s ${{ secrets.RAILWAY_SERVICE_ID }} --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
